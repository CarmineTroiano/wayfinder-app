<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <title>I Miei Viaggi - WayFinder</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>body { font-family: 'Poppins', sans-serif; background-color: #f8fafc; }</style>
</head>
<body class="bg-gray-50 h-screen flex flex-col">

    <nav class="bg-white shadow-sm px-8 py-4 flex justify-between items-center sticky top-0 z-50">
        <div class="flex items-center gap-3 text-indigo-600 font-bold text-xl cursor-pointer" onclick="window.location.href='/planner'">
            <i class="fas fa-paper-plane"></i> WayFinder
        </div>
        <div class="flex gap-4">
            <a href="/planner" class="bg-indigo-600 text-white px-5 py-2 rounded-lg font-bold text-sm hover:bg-indigo-700 transition">
                <i class="fas fa-plus mr-1"></i> Nuovo Viaggio
            </a>
            <a href="/logout" class="bg-gray-100 text-gray-600 px-4 py-2 rounded-lg font-bold text-sm hover:bg-red-50 hover:text-red-500 transition">
                <i class="fas fa-sign-out-alt"></i>
            </a>
        </div>
    </nav>

    <main class="flex-1 container mx-auto px-8 py-10 overflow-y-auto">
        <h1 class="text-3xl font-bold text-slate-800 mb-2">I Tuoi Viaggi</h1>
        <p class="text-slate-500 mb-10">Rivivi i tuoi itinerari o pianifica la prossima avventura.</p>

        <% if (trips.length === 0) { %>
            <div class="text-center py-20 bg-white rounded-3xl border-2 border-dashed border-gray-200">
                <i class="fas fa-map-marked-alt text-6xl text-gray-200 mb-4"></i>
                <p class="text-gray-400 font-medium">Non hai ancora salvato nessun viaggio.</p>
                <a href="/planner" class="text-indigo-600 font-bold hover:underline mt-2 inline-block">Crea il primo ora</a>
            </div>
        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% trips.forEach(trip => { %>
                    <div class="bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all duration-300 overflow-hidden group border border-gray-100 flex flex-col">
                        
                        <div class="h-48 bg-gray-200 relative overflow-hidden">
                            <img src="<%= trip.image %>" alt="<%= trip.destination %>" class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500">
                            
                            <div class="absolute top-3 right-3 bg-white/90 backdrop-blur px-3 py-1 rounded-full text-xs font-bold text-indigo-600 shadow-sm uppercase">
                                <%= trip.mood %>
                            </div>
                        </div>
                        
                        <div class="p-6 flex-1 flex flex-col">
                            <h3 class="text-lg font-bold text-slate-800 mb-1 line-clamp-1"><%= trip.title %></h3>
                            <p class="text-xs text-indigo-500 font-semibold uppercase mb-3"><%= trip.destination %></p>
                            
                            <p class="text-xs text-slate-400 mb-6 flex items-center gap-2">
                                <i class="far fa-calendar-alt"></i> <%= trip.startDate %> - <%= trip.endDate %>
                            </p>
                            
                            <div class="mt-auto grid grid-cols-4 gap-2">
                                <a href="/view-trip/<%= trip.id %>" class="col-span-1 bg-gray-50 text-gray-600 flex items-center justify-center rounded-xl hover:bg-gray-200 transition" title="Visualizza">
                                    <i class="fas fa-eye"></i>
                                </a>

                                <a href="/planner?tripId=<%= trip.id %>" class="col-span-2 bg-indigo-50 text-indigo-600 flex items-center justify-center rounded-xl font-bold text-sm hover:bg-indigo-600 hover:text-white transition">
                                    Modifica <i class="fas fa-pen ml-2"></i>
                                </a>

                                <button onclick="deleteTrip('<%= trip.id %>')" class="col-span-1 bg-red-50 text-red-500 rounded-xl hover:bg-red-500 hover:text-white transition">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </main>

    <script>
        async function deleteTrip(id) {
            if(!confirm("Vuoi davvero eliminare questo viaggio per sempre?")) return;
            try {
                const res = await fetch(`/api/delete-trip/${id}`, { method: 'DELETE' });
                const data = await res.json();
                if(data.success) window.location.reload();
            } catch(e) { alert("Errore durante l'eliminazione"); }
        }
    </script>
</body>
</html>